{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "system-icons-processing",
  "projectType": "library",
  "targets": {
    "deploy:fonts": {
      "cache": true,
      "inputs": ["assets", "fontGenerationToolchain"],
      "outputs": ["{workspaceRoot}/fonts"]
    }
  }
}
