/**
 * @deprecated Transition alias. The headless implementation IS the default API as of this
 * release — this subpath re-exports it unchanged. Import from
 * `@fluentui/react-icons-file-type` instead, and import
 * `@fluentui/react-icons-file-type/styles.css` rather than
 * `@fluentui/react-icons-file-type/headless/styles.css`.
 *
 * Kept for one release so existing headless adopters upgrade without a code change; removed
 * in the next major.
 */
export * from '../index';
